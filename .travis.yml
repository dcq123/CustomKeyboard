language: android
jdk: oraclejdk8

android:
  components:
    # use the latest revision of Android SDK Tools
    - platform-tools
    - tools

    # The BuildTools version used by your project
    - build-tools-25.0.2

    # The SDK version used to compile your project
    - android-25

    # Additional components
    - extra-android-m2repository

deplpy:
  skip_cleanup: true

script: ./gradlew assembleDebug
        curl -F "file=app/build/outputs/apk/app-debug.apk" -F "uKey=228125134fad0be589caaaac10d3c1ec" -F "_api_key=90706658a8a17c0e2c35329f96d8c565" https://qiniu-storage.pgyer.com/apiv1/app/upload